'use client'

/**
 * PWA Lifecycle & Standalone Detection Component.
 *
 * All service worker lifecycle, standalone detection, install prompt capturing,
 * and privacy-safe telemetry are unified in PwaInstallProvider.
 *
 * This component is maintained for backwards compatibility.
 */
export function PwaLifecycle() {
  return null
}
