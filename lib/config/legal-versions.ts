/**
 * Centralized legal document version constants.
 *
 * IMPORTANT: When updating Terms or Privacy Policy versions, change ONLY
 * these constants. Do not scatter version strings throughout the codebase.
 *
 * Future phases may introduce re-consent flows for returning users when
 * these versions change. For now, versions are recorded at signup time.
 */

/** Current Terms of Use version. Update when Terms are revised. */
export const CURRENT_TERMS_VERSION = '1.0'

/** Current Privacy Policy version. Update when Privacy Policy is revised. */
export const CURRENT_PRIVACY_VERSION = '1.0'
