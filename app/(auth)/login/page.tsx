import { LoginForm } from '@/components/auth/login-form'

export const metadata = {
  title: 'Entrar — AD-Marketplace',
  description: 'Acesse sua conta no AD-Marketplace',
  robots: 'noindex, nofollow',
}

export default function LoginPage() {
  return <LoginForm />
}
